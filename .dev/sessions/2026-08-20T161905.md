Synced agentics to current, and migrated .dev/roadmap.md to the split format.

- `AGENTS.md`: version tag now at the current agentics sync point; the dispatch table itself matches the canonical template.
- `.dev/roadmap.md`: each entry trimmed to priority/status, with depth moved to `.dev/docs/atlas/roadmap/<topic>.md`, cross-linked from the entry.
- `.dev/tech-debt.md`: its one entry reformatted to carry a standalone `fix:` field.
